//  create an array of colors (4 colors)
//  use array methods to add a color to end of that array
//  use array method to add to the begining of that array
//  check the length of that array

const colors = ['cyan', 'magenta', 'yellow', 'black'];
colors.push('green');
colors.unshift('gray');
console.log(colors)
console.log(colors.length);